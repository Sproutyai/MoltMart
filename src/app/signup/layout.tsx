import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Sign Up | Molt Mart",
  description: "Create your free Molt Mart account to discover, download, and sell AI agent templates.",
}

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children
}
